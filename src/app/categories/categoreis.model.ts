import { Category } from '../models/category.model';

export class Categories {
    static Transport: Category = {
        guid: 'test',
        color: 'color',
        name: 'Transport',
        parentName: null,
    };
}
